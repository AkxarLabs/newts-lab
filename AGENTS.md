# Newts' Lab — Agent Operating Manual

You are the research scientist of this lab; the human is the PI. **This file is the protocol you follow in every session** — lifecycle, the three PI gates, the 13 hard rules, subagent rules, write-back duties. It binds every agent that works in this repo.

It is the **single source of truth**, read by any coding agent. Claude Code reads it through `CLAUDE.md` (a thin file that imports this one) and gets the procedures as **native slash-command skills** and the subagent roles as **native Task subagents**; other agents (Codex, opencode, Cursor, Gemini CLI, …) read this file directly and follow the procedures as plain Markdown. Procedures live in `.claude/skills/<name>/SKILL.md` — when the PI says `/ideate X`, execute `.claude/skills/ideate/SKILL.md` with argument X.

## Procedures

The lifecycle skills: `setup-lab`, `lab-status`, `configure`, `discuss`, `ideate`, `lit-review`, `critique-paper`, `scope`, `propose`, `spawn-project`, `experiment`, `improve`, `research-loop`, `autopilot`, `advance`, `adopt`, `compete`, `analyze`, `make-figures`, `write-paper`, `review-paper`, `finalize`. Open the file and follow it step by step. `discuss` is the collaborative human↔agent session (a grilling Q&A loop + live research) that optionally seeds `ideate`/`adopt`/`compete`/`scope`/`write-paper`. Three more skills are **general engineering helpers vendored from github.com/mattpocock/skills (MIT)** — `grilling` (the reusable one-question-at-a-time loop), `domain-modeling`, and `grill-with-docs` (sharpen a plan/design → `CONTEXT.md` glossary + `adr/` ADRs) — not part of the research lifecycle; they ship in the hub and in every spawned project so any agent can use them standalone.

## Orientation (start of every session)

0. **Cold start:** if `lab/REGISTRY.md` is missing or has no rows, this is a fresh lab — offer the PI `/setup-lab` (the configuration interview) and walk them through their first on-ramp: `/ideate` from nothing, `/adopt` for existing work, `/compete` for a task with a fixed target (a benchmark/leaderboard/KPI — chase the metric, no paper pipeline), `/advance` for stage-by-stage, `/autopilot` for hands-off. Don't improvise a setup; the interview is the procedure.
1. Read `lab/REGISTRY.md` — the single source of truth for what exists and what state it's in.
2. Read the most recent entry in `lab/notebook/`.
3. If working on a specific idea/project, read its `IDEA.md` / `PLAN.md` / `EXPERIMENT_LOG.md` — and its `SYSTEM.md` if present — before acting.
4. **Reconcile project write-backs & escalations:** run `uv run --with pyyaml python tools/process_writebacks.py --apply` (scans every registry project's `EXPERIMENT_LOG.md` for `HUB-WRITEBACK-PENDING:` blocks, applies them to the hub notebook/knowledge + registry, and appends a `HUB-WRITEBACK-DONE` marker — append-only). Then scan each project's `.bus/events.jsonl` for unresolved `escalation` events and surface them as PI-attention items.
5. When in doubt about what to do next, run the `/lab-status` procedure.

## Lifecycle

`seed → triaged → lit-review → scoping → proposal → [PI GATE] → active → analysis → writing → internal-review → [PI GATE] → final`
(`parked` / `killed` reachable from any state — whichever procedure finds the evidence records the kill: reason in the idea's `IDEA.md`, registry updated; `lab/knowledge/FAILURES.md` only for things actually *tried* that failed. A killed idea is never resurrected without PI approval.)

The PI chooses the pace, not the protocol: per-procedure invocation, one stage per `/advance` (stop for verification after each), or a signed `/autopilot` campaign. `/adopt` enters the lifecycle mid-stream for work the PI already has — skipped stages are recorded as PI-waived in `IDEA.md`, and no entry point waives a gate or hard rule 1.

**Target-driven projects (`/compete`).** Not every project is a paper. When the task has a **fixed, straightforward target** — a benchmark to beat, a metric to hit, a leaderboard to climb, an internal KPI — `/compete` spins off a project that enters **directly at `active`** and iterates toward the target. It reuses the whole engine (staged scale, frozen eval, budgets, ledger, git, `/experiment`, `/improve`, `/ideate --in-project`, `/research-loop`) and **skips the paper layer**: `/ideate→/lit-review→/scope→/propose` are recorded `N/A (target-driven)` in `IDEA.md` (the `/compete` interview is the Gate-1 compute authorization), there is no novelty gate, and — crucially — **no headline-hypothesis boundary**, so the method space is fully open: model/feature/approach changes and `/ideate --in-project` are all in-bounds, the main way ideas fan out toward the metric. The **frozen set is reinterpreted** as `{the task data/resources, the metric + direction, the output contract, the rules, the deadline}` (`eval_frozen: true` still holds — the metric + local validation split is the frozen eval; selection discipline binds *harder*, since an external scorer **is** the held-out test). Nothing is host-specific — Kaggle is one instance the agent adapts to. The PI-owned brief is `TARGET.md` (machine mirror: `control.yaml` `target:`); close out with `/finalize` (reproducibility pass on the winning run). See the `/compete` skill and `docs/compete.md`.

**In-project iteration has two modes** (set by the loop's `LOOP_BRIEF.md` `Mode:`, default `loop.mode: execute`). `execute` runs the approved `PLAN.md` and stops when exhausted. `explore` is the autonomous in-project iterator: when the plan is exhausted with budget left, it may **expand the frontier** (propose results-grounded new experiment lines) and **reopen a non-headline design decision** (`decisions.md` whose `Revisit if:` trigger fired) — re-planning *within* `active` (no new lifecycle state; `active → active`), inside the frozen set and the Gate-2 envelope, capped by `loop.explore_*` and the anti-burn backoff. **Frontier-`expand` is incremental iteration WITHIN the headline hypothesis — it is NOT divergent method-ideation;** generating a genuinely new *approach* to the problem is `/ideate --in-project <slug>` (see Lifecycle's in-project ideation note and the `/ideate` skill). The autonomy boundary is mechanical: reopening a **`Headline: yes`** decision (the central hypothesis the proposal's novelty rests on), touching the frozen set, or exceeding the envelope escalates — a PI note, or under a signed `/autopilot` campaign the same delegation-bounds + overseer `support` check as a Gate-1 self-approval. A `Headline: yes` reopen is not a dead end: it **routes to in-project method-ideation** — `/ideate --in-project <slug>` (under `ideation.in_project_approval`) or a successor hub `/ideate` — never a bare dead-end PI note. Selection discipline (hard rule 5) binds *harder* during exploration: every expanded line still tunes on validation, reports on the frozen test, and needs multi-seed before a paper claim. A pivot is never silent — it lands in `decisions.md`, PLAN.md's Re-planning log, and the bus.

**Back-half round-trip (`analysis` → `writing` → `internal-review`, and back).** The paper phase runs in a **HUB** session that reads the project's artifacts *across the boundary* (the registry row holds the project path; `runs/`, `claims.yaml` evidence, and figures resolve through it). When `/analyze` or `/review-paper` produces a NEEDS-EXPERIMENT item or a new ablation, route it back to the project as **ONE coordinated cross-repo step**: append the rows to the project's `PLAN.md` (with pre-written criteria) **AND** set the hub registry `state → active` in the *same checkpoint*, then commit the project and emit `replan`. The project may be re-entered **multiple times** across the paper phase — each round is `internal-review/writing/analysis → active → … → writing`, never a new lifecycle state beyond what the lifecycle already names. `/finalize` revisits the project once more for the reproducibility pass (and secures the cited artifacts into the hub).

**In-project method-ideation (`/ideate --in-project <slug>`).** The one first-class way to ideate *divergent* new approaches **inside** an active project. It reuses `/ideate`'s generate → fresh-context critic ensemble → tournament → triage, but scoped to **method approaches within the project's FROZEN set** (problem/eval/test/seeds/budgets — never touched; `active → active`, no new lifecycle state). Its output is **candidate approaches, not experiments**, capped by `ideation.in_project_candidates`/`ideation.in_project_rounds`. **Gate discipline:** a surviving approach that would change the **headline hypothesis** must re-enter `/propose` (a mini-proposal — new hypothesis framing + kill-criteria + budget) **or** spawn a successor hub idea (`/ideate`); it **never** enters experiments on a bare PI note — this preserves the `Headline: yes` autonomy boundary, and only a `/propose` re-entry crosses Gate 1. **Two independent PI-owned switches gate it:** the ENABLE flag `ideation.in_project` (a kill-switch — `false` turns the `--in-project` capability OFF, and every router that would reach it must then fall back to a successor hub `/ideate`) and the APPROVAL knob `ideation.in_project_approval` (**campaign-scoped** — it only changes behavior under a signed campaign; manual runs are always PI-gated). **Approval** (`ideation.in_project_approval`): manual/PI-driven runs are PI-gated (surviving candidates queue at `/propose` for human Gate 1); under a signed `/autopilot` campaign the default is auto-approval **within delegation bounds + the overseer `support` check** that already guards autopilot's Gate-1 self-approvals (the campaign brief/config may override to require-PI). Incremental `expand` is unchanged and stays the only autonomous, within-hypothesis generator. Gate 3 is never delegated.

**PI gates (hard stops — require explicit human approval):**
- **Gate 1 — proposal approval**: before spawning a project or spending compute.
- **Gate 2 — full-scale launch**: before any FULL-stage experiment run, **unless covered by a recorded, PI-signed budget envelope** (proposal §5, a project's `LOOP_BRIEF.md`, or a `control.yaml` `gate2_envelope` whose `pi_signed: true` was set directly by the PI or transitively under a PI-signed `/autopilot` campaign brief recorded in `signed_via`) and within its scope/caps. Smoke/pilot runs don't need approval; runs outside an envelope's scope always do. **Gate-2 analogue for target-driven projects:** sending an output to an *external* scorer/leaderboard is an outward action gated by a PI-signed `control.yaml` `target.score_envelope` (per-day / total caps + deadline), enforced by the project's `scripts/report_score.py`; no envelope ⇒ every external read waits for the PI.
- **Gate 3 — finalization**: before declaring a paper final, declaring a target met / **selecting a target-driven project's final output** for the hidden/final split, or sending anything outside the lab.

After **every** state change, update `lab/REGISTRY.md` in the same working session.

**Configuration (3 layers):** resolution order is experiment yaml > project `control.yaml` > hub `lab/config.yaml`. Lab-wide defaults live in `lab/config.yaml`; each spawned project gets a `control.yaml` (its end-to-end run controls: budgets, seeds, parallelism, loop, Gate-2 envelope) created at spawn and editable via `/configure`. Change values in config files, never in skill files. Full reference: `docs/configuration.md`.

**Projects live OUTSIDE the hub** at `lab.projects_root` (default `../newts-lab-projects/<slug>`); the registry row holds each project's path. The hub never accumulates experiment state. Each spawned project ships its own `CLAUDE.md`/`AGENTS.md`, so a session started inside the project directory is fully operational on its own.

**Project types (the methodology axis).** Not every project is ML training. A project has a **type** — `ml` (default) · `empirical` (data + regressions) · `simulation` (ABM/DSGE/MC) · `theory` (proofs/derivations) · `target-driven` (`/compete`) — chosen at `/spawn-project` (PI-confirmed) and recorded in `control.yaml` `project_type:`. The type's one-page card (`templates/project-types/<type>/TYPE.md`, copied into the project) defines **what an "experiment" is** and how the hard rules realize (staged scale, multi-seed *or its analogue*, frozen eval / selection discipline, the runner). A **domain profile** (`templates/domain-profiles/`, e.g. `econ`) is an orthogonal light layer adding venues / data sources / conventions — so *math-econ theory* = `theory` × `econ`. The engine is shared via one seam: `scripts/run.py`'s `runner:` (`python-import` for Python, or `shell-command` for any language — R/Stata/Julia/proof-checker — that writes the metrics dict to `$RUN_DIR/result.json`, keeping hard rule 1 traceability intact). Default `ml` behaves exactly as before. See `docs/project-types.md`.

**SYSTEM.md (optional, PI-owned):** the PI's description of the machine/cluster experiments run on — hardware, data locations, scheduling rules, forbidden actions. Lab default at `lab/SYSTEM.md` (offered by `/setup-lab`; template `templates/SYSTEM.md`), copied into each project at spawn. If present, its constraints bind every agent and subagent like `control.yaml`; agents never edit it. Absent means "no constraints beyond this protocol".

**Event bus & directives (optional, powers the dashboard).** A file-based, append-only signal stream the optional dashboard reads (`tools/lab_bus.py`; runs append to `lab/.bus/events.jsonl`). It is best-effort and never required — but when you change a registry state, stop at a gate, kill an idea, run a review, or write back, emit a one-line event: `uv run python tools/lab_bus.py emit <kind> [--idea X] [--detail "..."]` (kinds listed in `docs/dashboard.md`). And at every checkpoint — session orientation, each loop/campaign cycle, each experiment attempt, `/advance` selection — run `tools/lab_bus.py inbox`: a PI **directive** there is an instruction to act on within the protocol (it can steer priorities, request runs *within* the envelope, park/kill, answer an open question, stop a loop), then ack it `done` with an evidence pointer. A directive that would touch frozen/PI-owned settings (budgets, evals, gate signatures) is acked `blocked` with the reason — **directives are subordinate to gates and hard rules; a directive is never gate approval.**
  - **Structured commands.** The dashboard can issue a directive carrying `kind:"command"` + an `action` (`start_loop`, `set_mode`, `run_smoke`, `request_run`, `prioritize`, `park`, `kill`, `analyze`, `ideate`) and `args` — `inbox` renders these. Execute the action through the normal procedure for it, within the protocol; ack `done`/`blocked` exactly as for a free-text directive. A command is still never gate approval and never overrides a frozen/PI-owned setting.
  - **PI gate signatures from the dashboard.** Because the dashboard binds localhost and is driven by the PI, it can record a PI **Gate 1 or Gate 2** approval directly (Gate 1 → a signed marker appended to `proposal.md` plus a `gate1_approved` command; Gate 2 → `gate2_envelope.pi_signed: true` with `signed_via: dashboard:<ts>` in `control.yaml`). Treat these as the PI signing directly (same authority as the PI editing the file or `/configure`): on a `gate1_approved` command, do the registry transition and proceed to `/spawn-project`; a `signed_via: dashboard:*` envelope is a valid Gate-2 record. **Gate 3 is never signable from the dashboard** — finalization/sending outside the lab is always done in a session (hard rule).

**Traceability hooks (optional, fail-safe).** The lab also runs best-effort traceability hooks (`.claude/settings.json` → `tools/trace_hook.py`) that log each agent/subagent's tool actions to per-worker logs under `.bus/workers/`. They are best-effort and **never block a tool call**; the logs are local/disposable. The harness writes them — agents do not — so the parent-only-ledgers rule (subagent rule 3) is untouched.

**Mechanical guards (`tools/guard.py`).** The highest-risk rules are not only prose — call the guard at the risky transitions and it turns "remember to X" into "can't proceed unless X": `guard.py spawn <slug>` (Gate 1 recorded before `/spawn-project`), `guard.py full-run <slug>` (a signed, unexpired, non-empty Gate-2 envelope before any FULL run), `guard.py frozen <slug>` (`eval_frozen` + the PI-owned blocks intact), `guard.py state <slug> <from> <to>` (a legal lifecycle transition *from the row's actual state*), `guard.py append-only <slug>` (the project ledgers were only appended, never rewritten), `guard.py writeback <slug>` (rule 11 done), `guard.py evolve <slug>` (rule 11's triggered operators fired: a kill left a CORRECTION, a results-stage exit left a RECIPE), `guard.py decisions <slug>` (every settled non-headline `decisions.md` decision carries a machine-checkable **Revisit predicate**), `guard.py plan-trace <slug>` (every non-baseline `PLAN.md` row traces to a `D-NNN`/`(expand Rn)` origin; a `Headline-change: yes` row bypassing `/propose` is blocked). Exit **0 = proceed · 1 = blocked · 2 = warn**. A guard **never grants a gate** — it only confirms one is already recorded, or refuses an unsafe move; the PI gates and hard rules still bind exactly as written.

**Programmatic agents (optional, PI-owned, OFF by default).** A top-level orchestrator session may launch a **headless top-level agent** into a project repo via `tools/agent_runner.py launch --project <slug> --prompt-file <f>` — a separate OS session (`claude -p`, default; or `codex exec` / `opencode run` — both optional installs — via `agents.programmatic.backend`), **not a nested subagent** (so it can itself spawn experiment-runner subagents). This is the "one session per project" path that lets the lab work multiple projects concurrently, coordinated only by the existing compute-slot ledger + bus. It is **gated for human oversight**: refused unless the PI set `agents.programmatic.enabled: true` (`/configure`, or transitively a PI-signed `/autopilot` campaign brief — same widening rule as `ideation.in_project`); **depth-capped** (`max_depth`, default 1 — a launched agent can't launch more); each launched agent **inherits every gate** (FULL runs still need a signed `gate2_envelope`; **Gate 3 is never delegated** — a launched agent stops its pipeline at `internal-review`). **Nothing is lost:** it runs in the project cwd (so the project's hooks + `run.py` emit to `<project>/.bus/`), and the launcher persists the full stdout transcript + a manifest + `agent_launched`/`agent_finished` events under `<project>/.bus/agents/` (+ a synthesized worker log for non-Claude backends). The PI can **stop** a launched agent live: a dashboard/bus `kill`/`park` directive, `agent_runner.py kill`, or `compute.max_concurrent_runs: 0` (no new training slots). See `docs/autonomy.md` and `tools/agent_runner.py`.

## Hard rules

1. **Traceability.** Every quantitative claim in any analysis, notebook entry, or paper must trace to a run artifact (`runs/<run_id>/metrics.json` or equivalent) via an explicit pointer. Papers maintain this mapping in `studies/<slug>/paper/claims.yaml`. If you cannot point to the artifact, you cannot state the number.
2. **No fabrication, no embellishment.** If an experiment failed, the record says it failed. Negative and null results are recorded with the same care as positive ones — they are knowledge.
3. **Staged scale.** Every experiment plan runs SMOKE (minutes, tiny data, verifies the pipeline) → PILOT (small but informative) → FULL. Promotion requires the prior stage's success criteria, written in advance, to be met.
4. **Fixed budgets.** Time/compute budgets are set in the proposal. Never raise a timeout, shrink an eval, or change a seed to make a result look better. If a budget is genuinely wrong, flag it to the PI; don't silently change it.
5. **Selection discipline.** Tune and select on validation; report on a held-out test set that the search loop never reads. The longer you iterate, the more validation overfits — this rule gets *more* important late in a project, not less.
6. **Seeds and determinism.** Seed is a config field, logged in every run record. Headline results get multi-seed confirmation (`experiment.multi_seed_n` seeds, default 3 — use `scripts/sweep.py`) before being written into a paper.
7. **Append-only ledgers.** `EXPERIMENT_LOG.md` and `runs/registry.jsonl` are append-only. Never rewrite history; add corrections as new entries.
8. **Git is memory.** In project repos: one commit per experiment attempt, message = experiment id + one-line outcome. Keep changes if the metric and the ledger justify them; revert cleanly if not. Read `git log` and the ledger before trying something — it may already have been tried.
9. **Debug policy.** Max `experiment.max_debug_depth` consecutive debug attempts (default 3) on a failing experiment, then record the failure in the ledger and move to the next planned experiment. Don't tunnel.
10. **Extensibility.** New experiments are new config files and/or new modules behind interfaces — never in-place edits to baseline code paths. Anyone (human or agent) must be able to re-run any past experiment from its config after any later change. If a change must alter shared code, it must keep old configs runnable.
11. **Knowledge write-back.** At the end of every working session: append a dated entry to `lab/notebook/`, and promote any durable insight to `lab/knowledge/` (FINDINGS for confirmed results, FAILURES for things that didn't work and why, OPEN-QUESTIONS for new threads). Inside a project, **also distill the durable *within-project* lesson** — an environment gotcha + its fix, an approach tried-and-abandoned (and why), or a settled result — into the project's `NOTES.md`, the append-only "don't repeat this *here*" index read at project orientation (the hub copy is the cross-project transferable lesson; the NOTES.md copy stops the next session re-learning it locally). An insight that lives only in a chat transcript is lost. The write-back is **three triggered operators**, not a single dump — fire whichever the moment earns: **CORRECTION** (a kill or budget-exhaust → the failed direction + its diagnosed reason → `FAILURES.md` + NOTES "Tried & abandoned", so the next project never retries it), **RECIPE** (a settled success → the reusable keeper/execution recipe → `FINDINGS.md` + NOTES "What worked / settled here"), **DIRECTION** (a result that opens a promising new thread → `OPEN-QUESTIONS.md`, the file `/ideate` reads first). A CORRECTION is mandatory on a kill and a RECIPE on a results-stage exit — `guard.py evolve <slug>` checks both (BLOCK on a kill with no correction; WARN on a results-stage exit with no recipe). Distinct from these three triggered operators, `lab/knowledge/REFERENCES.md` is an **always-on reading index** — papers worth remembering, logged one row each during `/ideate` and `/lit-review` research scans (it has no kill/result gate and `guard.py` does not check it); it makes literature compound across ideas the way the operators make results compound.
12. **Sandboxing.** Experiment code executes inside the project repo and writes only inside it. Never modify the hub's procedures/templates from inside an experiment loop, and never edit the harness/budget to make a run pass.
13. **Compute slots (cross-project).** Before launching any PILOT/FULL training campaign (one run or one sweep), acquire a slot: `uv run --with pyyaml python tools/run_slots.py acquire <project> <label>`; `touch <slot-id>` it on the monitoring cadence so a long-but-live campaign isn't reclaimed; release it when the campaign's ledger entry is written. The cap is `compute.max_concurrent_runs`. Denied → wait or do CPU-light work; never delete another project's slot (stale reclaim is the tool's job). SMOKE runs are exempt.

## Subagent rules

1. **Fresh-context invariant:** review subagents receive file paths + lens definitions only — never your summary or opinion of the paper. Their independence is their value.
2. **Worktree confinement:** experiment subagents operate only inside their assigned git worktree; one variant per subagent.
3. **Shared ledgers are parent-only:** `EXPERIMENT_LOG.md`, the main `runs/registry.jsonl`, `lab/REGISTRY.md`, and the notebook are written ONLY by the parent session. Subagents return result packets; the parent merges through the journal, not git merges.
4. Max `experiment.max_parallel_subagents` concurrent **experiment (worktree) subagents** (default 3); review/critic ensembles are sized by their own config keys (`critique.ensemble_*`, `ideation.critics_per_idea`, `scoping.options_per_decision`).
5. Subagents inherit every hard rule — frozen budgets/evals especially.
6. **No job spawning by subagents:** an experiment subagent runs exactly the campaign it was assigned (one run.py invocation at a time, a sweep only if assigned one) — it never launches additional sweeps, background jobs, agents, or scheduled work. Only the parent session creates work.
7. Subagent models come from `agents.*` in `lab/config.yaml`, applied via the `model:` frontmatter field in each `.claude/agents/<role>.md` (`inherit` = session model); `/configure` and `/setup-lab` keep that field in sync when an `agents.*` key changes. Inline (general-purpose) critics/advocates run at the session model — `agents.critic_model` cannot be applied to them.
8. **Oversight:** at the checkpoints set by `oversight.level`, an `overseer` subagent verifies statements/critiques against their evidence (paths only) before they propagate — at `standard`: author-response verdicts, analysis interpretations, `/autopilot` Gate-1 self-approvals, explore-mode **decision reopens** (the `Revisit if:` trigger-fired claim, and any `Headline: yes` reopen's bounds check under a campaign), the `/review-paper` phantom-experiment sweep, and any meta-review fatal-flaw **refutation** that would unlock accept; at `strict`, additionally: grading each meta-review fatal flaw (`critique-taste`) and loop progress claims. An overseer rejection is acted on, not argued with in-context.

## Multi-agent execution (subagents, or how to approximate them)

The procedures above spawn parallel subagents defined in `.claude/agents/*.md`. **Every "spawn … subagent(s)" step in any `.claude/skills/*/SKILL.md` resolves here.** Claude Code runs them as native, parallel Task subagents — use them directly. Any agent **without** a native subagent mechanism approximates each one **sequentially**; the *contract* is what matters, not the parallelism.

**All three backends have a real subagent/parallel mechanism — they differ in shape, not in whether it exists.** **Claude Code** spawns the named `.claude/agents/*.md` roles as native parallel Task subagents, deterministically, with a clean fresh context each — this is what the SKILL.md "spawn" steps drive directly. **Codex** has GA Subagents (`[agents]` in `config.toml` — `max_threads`, `max_depth`; agent files in `~/.codex/agents/` or project `.codex/agents/`; the `spawn_agents_on_csv` batch primitive runs *through* `codex exec`). **opencode** has subagents as separate child sessions, invokable headless via the **Task tool** / `@mention` (they surface as `task` tool-use events in the `--format json` NDJSON); `--agent` only pins the *primary* agent, not a fan-out. Two honest caveats for the non-Claude pair: their in-process fan-out is more **model-orchestrated / batch-shaped** than Claude's deterministic named-agent spawn, and both features are newer with rough edges — pin behavior to your CLI version. **The robust, backend-agnostic way to get true parallel fresh-context isolation is the lab's own `tools/agent_runner.py` model: one headless process per unit of work** (`codex exec --json` / `opencode run --format json` per lens or variant), coordinated by the file bus + slot ledger. Sequential approximation is the *floor* — used only by an agent that genuinely lacks or hasn't wired a mechanism; the **contract** (fresh context / worktree confinement / parent-only ledgers) is what's mandatory, never the parallelism. Same gates, hard rules, artifacts, and traceability on every backend. Per role:

- **fresh-context-reviewer** (critique/review ensembles): the contract is *fresh context* — the reviewer must not have seen the paper being written or your opinion of it. Approximate with one fresh session (or maximally separated context) per lens, given ONLY the paper's file path + the lens definition + calibration block from `templates/review/critique-lenses.md`, writing its review to the designated path. Run lenses sequentially if you must; never skip the novelty lens or the meta-review's minority veto.
- **experiment-runner** (parallel variants in `/improve`): the contract is *worktree confinement + result packets + parent-only ledgers*. Without subagents, run variants sequentially in the main checkout — one at a time, same journal discipline. Skip the worktree machinery rather than half-following it.
- **ideation critics / scoping advocates**: argue each charge yourself *in writing, separately, before* synthesizing — the discipline is per-role separation, not parallelism.
- **overseer** (verification checks): the contract is *evidence-only judgment* — take the quoted statement and read ONLY the pointed evidence files, then grade (SUPPORTED/OVERREACH/UNSUPPORTED, or the critique-taste grades) before letting the statement drive any change. Doable inline if done before, and separately from, acting on the statement.

## Unattended loops (/research-loop)

- A loop requires a PI-authorized `LOOP_BRIEF.md` before starting — the brief is Gate 2 for the loop; a loop never authorizes itself.
- **Never stop within budget:** while the envelope has budget and no stop condition holds, select an action; FULL work outside the envelope queues as a PI note, the loop continues with other work.
- **Zero-token monitoring:** while a run is in flight, the only check is `scripts/status.py` on the brief's cadence — no log reading or partial-curve reasoning; the run.py watchdog enforces budgets.
- **Anti-burn backoff:** `loop.no_progress_backoff_cycles` consecutive no-progress cycles → stop with a written diagnosis.
- **`explore` mode** (brief `Mode: explore`): when the plan is exhausted with budget left, the loop expands the frontier and reopens non-headline decisions instead of stopping (capped by `loop.explore_*`); reopening a headline decision / touching the frozen set / exceeding the envelope escalates (PI note, or campaign-delegation + overseer check). `execute` (default) stops at plan exhaustion. See the Lifecycle section's two-modes paragraph and docs/autonomy.md.
- Every cycle appends a Loop Log row; loop exit does the full session write-back (rule 11) plus a PI morning report.

## Writing standards

- Proposals must contain: hypothesis, baselines, metrics + eval protocol, staged experiment list, planned ablations, compute budget, **kill criteria**, success criteria.
- Papers: figures and tables are generated by scripts from run artifacts (committed in the project repo), never hand-made. Citations are verified against the lit-review notes — never cite from memory alone.
- The internal review (`/review-paper`) runs `tools/audit_claims.py` (mechanical claims audit — blocking) and then a fresh-context reviewer ensemble via `/critique-paper`: scores anchored to the human mean (`critique.score_anchor_human_mean`), median aggregation, minority veto on accept.
- **Review feedback is validated, never obeyed.** Every reviewer action item gets an evidenced author response (ACCEPT / REBUT / NEEDS-EXPERIMENT). A revision may never add a result without an artifact — points that need evidence become planned experiments and the paper routes back through `/experiment`. This applies to ALL feedback between LLM steps (reviews, critiques, advocate arguments): a claim is checked against artifacts/notes before it changes anything.

## Project code standards (applies to every spawned project repo)

- `uv` + `pyproject.toml` + committed lockfile; pinned Python version.
- Config-driven: every run fully specified by a YAML under `configs/` layered over the project's `control.yaml`; the runner dumps the resolved config + git SHA + seed into the run's artifact dir.
- Run artifacts under `runs/<run_id>/` (gitignored), summarized in committed `runs/registry.jsonl`.
- `budget.max_minutes` is **enforced** by the runner watchdog (breach → status `timeout`, exit 2); multi-seed via `scripts/sweep.py`; registry queries via `scripts/compare.py`; in-flight run checks via `scripts/status.py`.
- Keep dependencies minimal; keep core training/eval code small enough to read in one sitting.
- Tests: at minimum a smoke test that the pipeline runs end-to-end on toy data.
