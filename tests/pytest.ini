[pytest]
# Hermetic hub-tools suite. The default OS temp base (…/Temp/pytest-of-<user>) is not
# writable/cleanable in some sandboxes, so anchor tmp_path under tests/.pytmp (gitignored).
# basetemp is resolved relative to the rootdir (this directory).
addopts = -q --basetemp=.pytmp
testpaths = .
