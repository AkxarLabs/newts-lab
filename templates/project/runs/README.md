# Runs

Per-run artifact directories land here (gitignored). The committed record is
`registry.jsonl` (created on first run; append-only, one JSON line per run) plus
`../EXPERIMENT_LOG.md`. The artifact dir is the source of truth for every number
cited anywhere — see `claims.yaml` in the paper.
