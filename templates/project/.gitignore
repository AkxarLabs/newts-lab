# Run artifacts are local; the committed record is runs/registry.jsonl + EXPERIMENT_LOG.md
runs/*
!runs/README.md
!runs/registry.jsonl

# Dashboard event bus + directive inbox + loop heartbeat (runtime state)
.bus/

# guard.py append-only baselines (runtime state)
.guard/

data/
__pycache__/
*.pyc
.venv/
.pytest_cache/
