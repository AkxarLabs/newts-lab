\documentclass[11pt]{article}

% ── ACL ARR (ACL Rolling Review) ─────────────────────────────────────────────
% Requires acl.sty + acl_natbib.bst in this directory (see ../README.md for the URL).
% Two-column. ARR submissions use review mode (line numbers, anonymized).
% Review:       \usepackage[review]{acl}
% Camera-ready: \usepackage[]{acl}            % de-anonymized, no line numbers
\usepackage[review]{acl}

\usepackage{times}
\usepackage{latexsym}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{amsmath,amssymb}

\title{{{title}}}
\author{%
  % Anonymous in review mode (the style suppresses author info).
}

\begin{document}
\maketitle

\begin{abstract}
% Hypothesis, method, headline result (with claim ID in a comment), implication.
\end{abstract}

\section{Introduction}
% Gap (from lit review) -> hypothesis -> contributions as bullet claims, each backed by claims.yaml.

\section{Related Work}
% Cite ONLY from studies/{{slug}}/lit-review.md notes.

\section{Method}

\section{Experimental Setup}
% Datasets, baselines, metrics, val/test protocol, seeds, compute. Must match the frozen proposal.

\section{Results}
% Every figure/table generated by a committed script in the project repo (scripts/figures/).
% Annotate each number with its claim ID in a LaTeX comment: % C001

\section{Ablations}

\section{Limitations}
% ACL requires a Limitations section (unnumbered, not counted toward the page limit).

\section{Conclusion}

% ACL ARR also expects an Ethics/Broader-Impact statement where relevant.

\bibliographystyle{acl_natbib}
\bibliography{references}

\end{document}
