\documentclass{article}

% ── NeurIPS ──────────────────────────────────────────────────────────────────
% Requires neurips_2025.sty in this directory (see ../README.md for the URL).
% Submission:  \usepackage{neurips_2025}              % anonymized + line numbers (the default — blind review)
% Drafting:    \usepackage[preprint]{neurips_2025}   % author names VISIBLE, "Preprint" banner (arXiv)
% Camera-ready:\usepackage[final]{neurips_2025}       % author names visible, no banner (/finalize switches)
\usepackage{neurips_2025}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{microtype}
\usepackage{nicefrac}
\usepackage{xcolor}
\usepackage{url}
\usepackage{hyperref}
% neurips_2025 loads natbib itself — do NOT \usepackage natbib again (option clash).

\title{{{title}}}

\author{%
  % Anonymized at submission by the style; fill for preprint/camera-ready.
}

\begin{document}
\maketitle

\begin{abstract}
% Hypothesis, method, headline result (with claim ID in a comment), implication.
\end{abstract}

\section{Introduction}
% Gap (from lit review) -> hypothesis -> contributions as bullet claims, each backed by claims.yaml.

\section{Related Work}
% Cite ONLY from studies/{{slug}}/lit-review.md notes.

\section{Method}

\section{Experimental Setup}
% Datasets, baselines, metrics, val/test protocol, seeds, compute. Must match the frozen proposal.

\section{Results}
% Every figure/table generated by a committed script in the project repo (scripts/figures/).
% Annotate each number with its claim ID in a LaTeX comment: % C001

\section{Ablations}

\section{Limitations}
% NeurIPS also wants a paragraph on broader impacts where relevant.

\section{Conclusion}

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
